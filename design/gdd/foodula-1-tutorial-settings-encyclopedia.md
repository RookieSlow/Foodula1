# Foodula1 — 新手教程、设置与游戏百科

> 状态：In Progress  
> 实现基线：2026-08-27  
> 场景配置：`tutorial_le_mans_uk_v1`

## 目标与边界

主菜单提供“新手教程”和“设置”入口。教程是隔离比赛模式：固定使用 UK 车型和
`le_mans_old_mulsanne`，关闭科技树、科技点、车手技能、赛事奖励与正常进度写入。
正式勒芒 JSON 和正常比赛规则不得为教程改写；教程通过独立场景定义、步骤门控和
脚本检查点复用现有规则。

## 教学顺序

引导先说明目标，再按玩家实际操作顺序推进。当前纯状态机的权威顺序为：

1. 目标与界面；回合流程。
2. 挡位与必须打出的速度牌张数；速度牌与移动。
3. 抽牌、手牌、弃牌与洗回牌库。
4. 支付热量；热量牌与冷却；缺牌惩罚。
5. 弯道限速与打转；天气影响。
6. 回合结束尾流判定，只有后车受益。
7. 维修区预选；下一回合延迟执行。
8. UK 司康与英式早餐茶的实际效果。
9. 总结复习；重置到练习状态；完成一整圈自由练习。

`TutorialStepId` 是顺序权威；UI 只能报告已完成的 `TutorialAction`，不能直接跳过
状态。错误操作返回期望动作且不推进，以避免软锁和状态漂移。

当前 `TutorialGuideUI` 从步骤定义读取章节、标题和渐进式操作指引。正文先用一两句说明“为什么
要学”，再以 `现在场上 -> 轮到你了 -> 完成后 -> 没反应？` 交代脚本状态、唯一操作、即时结果与
安全恢复，不再连续使用“目标/当前/操作/成功/卡住时”的说明书式命令。完成一步后，下一步以
“做得好”保留刚才的可观察结果，避免自动推进吞掉反馈。阅读步骤继续使用明确按钮文案；实际
操作仍只能由 `MVPGameManager` 收到对应比赛事件后调用 `TutorialRuntimeDirector` 推进。

每个 `TutorialStepDefinition` 还声明一个语义化 `TutorialFocusTarget` 和独立的
`focusIntroduction`。`TutorialFocusHighlightUI` 将目标解析到实时 HUD、手牌中特定 UK 卡牌、
维修选择或赛道区域，用四块无射线遮罩留出聚光窗口，并显示金色边框和一条短说明。高光层不
拦截鼠标、不改变教程状态；特殊牌和维修面板会随运行时对象重建重新解析。开启“减少动态”时
边框保持静态，不播放呼吸变化。

正文面板使用纯 `TutorialGuideLayoutRules` 根据当前分辨率计算安全边距、宽高和正文尺寸；
1920×1080、1280×720、960×540 使用完整布局，854×480 使用紧凑字号。玩家可随时点击右上角
“收起指引”，将面板缩为仅显示标题、章节进度和“展开指引”的窄条，以查看右侧牌堆、热量、
天气或赛道；展开/收起只改变表现，不发送教程动作，也不改变比赛输入门或步骤状态。

教程表现层的可视化编辑权威为 `Assets/Resources/Prefabs/UI/TutorialOverlay.prefab`。根节点
`TutorialOverlayAuthoring` 保存 16 步展示文案及练习/完成态文案；`TutorialGuidePanel` 的
RectTransform、字号和按钮位置由 Prefab 人工布局决定；`TutorialFocusHighlight` 暴露目标留白、
边框、短说明偏移/尺寸和外部压暗强度。运行时优先绑定当前 `RaceCanvas` 下已有实例，否则从
Resources 自动实例化该 Prefab；收起后再次展开会恢复人工布局快照，不重新套用旧的自动排版。

指引按 `起步 -> 基础驾驶 -> 卡牌循环 -> 热量管理 -> 赛道规则/互动 -> 维修区 -> UK 特殊牌
-> 总结` 分组。特别处理两个容易产生流程误解的跨回合步骤：维修区预定后要求等待下一回合
自动执行；司康完成后，英式早餐茶步骤明确要求先结束当前回合，再等待下一回合检查点发牌。

### 同类教程研究基线（2026-08-28）

- Legends of Runeterra 官方新手流程按理解程度逐步开放 AI、Challenges 与 PVP；其首批 AI 对局
  还会固定匹配、抽牌和 AI 行为。Foodula1 对应采用“一步一个概念 + 精确脚本状态”。
- MARVEL SNAP 官方新手体验用独立对局教授卡牌协同，并延后回合计时压力。Foodula1 对应采用
  单机制聚光、阅读步骤暂停输入，待玩家实际操作后再推进。
- Hearthstone 官方把基础学习包装成轻量教程任务与 Apprentice Track。Foodula1 对应采用短句、
  鼓励式结果和“先看这里/轮到你了”的赛事实况口吻，同时保留精确规则信息。

以上是从官方说明提炼出的设计原则，不复制任何游戏的具体文案或玩法规则。参考：
[LoR 1.0 notes](https://playruneterra.com/en-us/news/patch-1-0-notes/)、
[LoR 2.11 prologue](https://playruneterra.com/en-us/news/game-updates/patch-2-11-0-notes/)、
[Marvel Snap 2026-05-19 notes](https://marvelsnap.com/patch-notes-may-19th-2026/)、
[Hearthstone new-player guide](https://news.blizzard.com/en-us/article/24166335/launch-into-heroes-of-starcraft)。

## 状态机与恢复

`Guided -> Practice -> Completed` 是正常路径；任意阶段可 `Exited`。引导允许跳过至
练习，设置页可重播/重置到第一步。练习圈支持重新开始，并重新装载教程牌组、热量、
天气、对手与车辆位置。错误操作保持当前步骤；场景执行失败时回滚到当前步骤的脚本
检查点，而不是继续推进。

运行时完成总结或点击“跳过引导”后，会销毁当前比赛状态并以 `Practice` 初始相位重建
完整 Race 会话：圈数归零、车辆回到起终点、重新创建精确牌组和 6 热量池、恢复教学
对手并应用 `cloudy`。练习固定一圈且不改写 `GameConfigSO.totalLaps` 或勒芒 JSON。
面板在练习/完成态提供重新开始、重播引导和退出；玩家过线后立即记录完成并结束比赛。

引导内的高风险操作步骤另有 8 个 `TutorialPlayerCheckpoint`。检查点在步骤进入时排队，
只在当前挡位输入门或下一回合边界应用，避免移动/反应/弯道协程中途改写状态。每个检查点
精确重建普通牌顺序、手牌张数、手牌/弃牌热量、引擎余量、位置和挡位，并清除上一步留下的
打转跳过、退赛及维修预约标记。缺牌步骤固定为 1 张速度牌 + 1 引擎热量；打转步骤固定在
Dunlop 弯前 8 格、手牌前两张为 3+2、6 热量在弃牌区，G2 冷回 1 后仍不足支付超速 2；
Weather 检查点会清除打转跳过，确保下一步可继续操作。

## 确定性与隔离

- `CardDeck.InitializeExactOrder` 将列表首项定义为下一张牌；不使用随机种子。
- 精确模式下主动洗牌无效，弃牌洗回保持弃置先后次序；正常 `InitializeDeck` 仍使用
  原 Fisher-Yates 随机路径。
- `TutorialLaunchState` 只覆盖当前教程 Race 的赛道解析，不写入或清空玩家已有的
  Quick Race 赛道选择；返回主菜单或开始普通比赛时清除教程请求。
- 教程牌组精确包含 12 张速度牌 `[1×3, 2×5, 3×3, 4×1]` 与 UK 两种特技牌各 2 张。
- 运行时玩家固定 UK、6 热量、零科技和零车型被动数值；UK 特殊牌仍保留用于教学。
  教学对手使用独立的 12 张纯速度精确牌组和固定规则随机源。
- 天气由步骤脚本设置；尾流步骤把领航车放在 42 格、玩家放在 40 格，以 2 格距离
  进入现有尾流回合结束判定。该脚本不修改普通比赛尾流规则。
- 领航车 cue 在进入尾流步骤时只入队，直到所有赛车完成基础移动后、正常尾流解析前才
  实际定位，避免后续移动破坏固定 2 格距离。
- 正式勒芒 JSON 没有维修区。教程配置因此声明入口 132、出口 4，并由
  `TutorialCheckpointRules.CreateVirtualPitRuleNodes` 构造只供 `PitLaneRules` 读取的规则视图；
  正式 142 节点列表和 JSON 均不修改。玩家在入口前 2 格获得确定性预约窗口，越过入口后
  仍复用正常“下一回合执行”规则。
- 教程会话不得调用 RP、解锁、车手 XP、赛事奖励或正常赛事进度持久化入口。

## 日志

每次开始、步骤完成/拒绝、跳过、练习开始/重启/完成和退出均产生可排序的
`[TUTORIAL] seq=... event=... step=... detail=...` 记录。运行时接入
`RaceTestLogWriter` 时同时记录固定牌序、天气 cue、对手位置、检查点牌区/引擎状态、
虚拟维修入口/出口和练习圈终态。

## 设置与百科

设置至少持久化主/音乐/音效音量、显示模式、分辨率、动画速度或减少动态，以及教程
重播/重置。项目当前没有真实音频、AudioMixer 或音频服务，因此音量字段只能作为
诚实的可扩展数据，不能宣称已控制不存在的混音通道。

当前设置以单一版本化键 `Foodula1.Settings.V1` 保存。`GameSettingsData` 负责默认值、
范围归一化和版本边界，`GameSettingsRepository` 隔离 PlayerPrefs，`GameSettingsRuntime`
只应用当前真实能力：全屏窗口/窗口模式、分辨率、比赛逐格停顿与镜头延迟的动画速度，
以及按钮反馈/可选延迟的减少动态。三路音量在界面明确标注“预留”，只持久化不伪造
AudioMixer 应用。教程完成标记也位于该设置键中，仅改变主菜单“新手教程/重播新手教程”
文案，可单独重置，不接触科技、车手、RP、解锁或赛事进度存档。

百科采用 `Resources/Configs/encyclopedia_zh.json` 的版本化数据目录和稳定条目 ID，
`EncyclopediaCatalog` 统一负责加载、必需主题、重复 ID 与正文完整性校验。设置覆盖层中的
“游戏百科”打开独立阅读器，支持上一条/下一条循环浏览和长文滚动，不把规则正文硬编码在 UI。
当前 17 条覆盖回合流程、三种卡牌类型、挡位、牌区与洗牌、热量/冷却、缺牌、弯道/打转、
尾流、五种天气、维修区、六队车型、12 种特殊牌、12 名车手、科技树和主要 UI 术语。
特殊牌/车手/天气条目保存 `relatedRuleIds`，EditMode 测试反向对照运行时目录；车手条目明确
标注签名技能当前只存在于配置，尚未接入比赛结算。

## 逐项验收

- [x] 独立配置明确 UK、勒芒、零科技/技能/奖励/进度写入。
- [x] 精确起手、后续抽牌与弃牌洗回不依赖随机种子，并有 EditMode 测试。
- [x] 16 个引导步骤顺序、错误门控、跳过、重播、练习转换有纯层测试。
- [x] 脚本天气和确定性尾流领航车配置已建模。
- [x] 主菜单教程入口与隔离启动已接入：勒芒、UK、6 热量、零科技/车型被动、精确起手，
  且普通 Quick Race 保持原赛道、随机牌组、科技和车型增益路径。
- [x] 教程完赛展示路径不调用 RP 或车手 XP 计算；RaceTestLog 写入场景、牌序、天气和
  对手脚本就绪事件。
- [x] Runtime Director、引导面板、一次性天气/对手 cue 和各机制真实事件钩子已接入；
  加入检查点后的教程定向 `20/20` 与全量 EditMode `503/503` 通过。
- [x] 16 步运行时指引已改为渐进、鼓励式短句，每步仍精确声明场上状态、唯一操作、成功反馈与
  恢复；13 类语义高光目标全部有独立短说明，挡位/天气/维修区/两张 UK 卡均固定到对应区域。
  教程定向 `26/26`、全量 EditMode `509/509` 通过，0 失败、0 跳过。
- [x] 指引面板已增加响应式安全区域和收起/展开交互；收起时保留标题与步骤进度，且不触发任何
  教程状态变化。常用 16:9 尺寸边界由纯布局测试覆盖，实际画面遮挡仍需 Play Mode 确认。
- [x] `TutorialFocusHighlightUI` 已接入无输入拦截的聚光窗口、边框和独立机制短说明；减少动态时
  边框静止，运行时对象可重新解析。实际像素位置、遮罩边界和文字换行仍需 Play Mode 确认。
- [x] 指引表现层已转换为 `TutorialOverlay.prefab`：16 步正文和练习文案可在 Inspector 修改，
  面板/按钮/聚光短说明可在 Prefab Mode 或 `RaceCanvas` 实例中手工布局；运行时保留自动加载回退。
- [x] 为支付/冷却热量、缺牌、打转恢复、维修区和两张 UK 特殊牌建立 8 个确定性检查点；
  正式勒芒无维修区时使用教程专用 132/4 规则视图复用正常 `PitLaneRules`，不修改赛道数据。
  教程定向 `20/20`、全量 EditMode `503/503` 通过。
- [x] 引导后重建一圈练习；完成、重启、重播引导和退出均接入运行时与教程日志，
  练习使用阴天与精确牌组且继续跳过 RP/XP/解锁/正常进度写入。
- [x] 主菜单设置入口、版本化设置持久化、显示/分辨率应用、动画速度/减少动态和教程
  完成标记重置已接入；音量诚实保持数据预留。设置自身测试仍为 `6/6`，当前全量为 `503/503`。
- [x] 数据驱动百科以 17 条版本化 JSON 覆盖全部必需主题，设置内可滚动浏览；ID、必需主题、
  12 张特技牌、12 名车手和 5 种天气均有运行时目录对照测试，百科定向 `6/6`、
  当前全量 EditMode `503/503` 通过。
- [ ] 完整引导到练习的受控 Play Mode、渐进式面板与高光的实际分辨率/遮挡验收、最终 Console/日志和
  正常 Quick Race 人工防回归仍待完成。
